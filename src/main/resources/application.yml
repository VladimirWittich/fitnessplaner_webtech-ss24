spring:
  datasource:
    username: ${DATASOURCE_USERNAME}
    password: ${DATASOURCE_PASSWORD}
    url: jdbc:postgresql://ddpg-cp5p1oljm4es73a3pr70-a.frankfurt-postgres.render.com:5432/fitnessplanerdatabase
    driver-class-name: org.postgresql.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
      jdbc:
        time_zone: UTC
    show-sql: false # Set true for debugging
    open-in-view: false
    hibernate:
      ddl-auto: update
  sql:
    init:
      platform: postgres
      mode: always
